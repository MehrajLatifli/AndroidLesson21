<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidLesson21" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="windowSplashScreenBackground">@color/White</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/logo2</item>

        <item name="postSplashScreenTheme">@style/Theme.AndroidLesson21</item>

        <item name="windowSplashScreenAnimationDuration">500</item>

        <item name="android:statusBarColor">@color/BlazingOrange</item>
        <item name="android:windowLightStatusBar">true</item>

    </style>

    <style name="Theme.AndroidLesson21" parent="Base.Theme.AndroidLesson21" />
</resources>